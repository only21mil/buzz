# Later canon patch

Do not apply this file while `FROZEN_OWNER_STOP` controls the program. After Victor or Rachel authorizes the named canon scope, make these exact edits in `/home/victor/Obsidian/Victor/Agent-Shared/`:

1. Create `PLANS/BUZZ_CI_FULL_MIGRATION_STATUS.yaml` from the reviewed candidate ledger.
2. In the separately authorized routing-surface step, replace `/home/victor/work/ci-mig/TASKBOARD.md` with the renderer output. Do not hand-edit the generated board.
3. Update `working-context.md` at the head with the controlling owner-stop event, authoritative `buzz/main` SHA, last source-bound deployed SHA, inactive Mempool/Genesis state, and candidate-ledger path.
4. Add a dated `decisions-log.md` entry that makes the YAML ledger authoritative for routing, records the evidence-precedence order, requires contradictions to resolve to `UNKNOWN`, and names both qualified legacy aliases.
5. Update `knowledge/wiki/systems/buzz-platform-2026-08-25.md` with a clearly dated successor-state section for current main, current deployment, inactive Mempool/Genesis state, production composition closure, and the generated-board rule.
6. Update `knowledge/index.md` only if the status ledger or task board is indexed there under the maintained schema.
7. Sweep the instruction-surface registry and generated surfaces for the stale all-`NOT_STARTED` board, old main or deploy claims, active Mempool/Genesis claims, and unqualified legacy lane labels. Record the readback result in the dated decision entry.

The canon wave remains incomplete if any required path is unwritable or if the renderer/checker candidate lacks its required independent review before activation as a routing control.
