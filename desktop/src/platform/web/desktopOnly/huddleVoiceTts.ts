// Lane 3 family module: huddleVoiceTts. Filled by the family producer; only this file (plus
// its sibling *.test.mjs) is owned by that producer.
export function registerHuddleVoiceTtsCommands(): void {
  // TODO(lane3): populated by producer
}
