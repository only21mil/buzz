// Lane 3 real-implementation module: relayCryptoSocial. Filled by the module producer; only
// this file (plus its sibling *.test.mjs) is owned by that producer.
import type { BrowserIdentityManager } from "../identity";

export function registerRelayCryptoSocialCommands(
  _identity: BrowserIdentityManager,
): void {
  // TODO(lane3): populated by producer
}
