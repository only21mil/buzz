// Lane 3 family module: agentsRuntimeBuilderlab. Filled by the family producer; only this file (plus
// its sibling *.test.mjs) is owned by that producer.
export function registerAgentsRuntimeBuilderlabCommands(): void {
  // TODO(lane3): populated by producer
}
